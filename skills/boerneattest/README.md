# boerneattest

Skill til read-only administration af børneattester for frivillige i Bredballe IF via Conventus.

```powershell
$env:PYTHONPATH = "skills/boerneattest/scripts"
python -m agent --help
```

OpenClaw/Linux kan whiteliste `skills/boerneattest/bin/boerneattest.sh`.
